<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.covert.verify360">

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="false" />
    <uses-feature android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false"/>

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".VerifyApp"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:screenOrientation="portrait"
            android:name=".SplashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:screenOrientation="portrait"
            android:name=".LoginActivity"
            android:configChanges="orientation|keyboard|keyboardHidden"
            android:theme="@style/NormalTheme" />

        <activity
            android:screenOrientation="portrait"
            android:name=".MainActivity"
            android:theme="@style/NormalTheme"
            android:configChanges="orientation|keyboard|keyboardHidden" />

        <activity
            android:screenOrientation="portrait"
            android:name=".CaseBusinessVerificationActivity"
            android:theme="@style/NormalTheme"
            android:configChanges="orientation|keyboard|keyboardHidden"
            >

        </activity>

        <activity
            android:screenOrientation="portrait"
            android:name=".CasePaySlipVerificationActivity"
            android:theme="@style/NormalTheme"
            android:configChanges="orientation|keyboard|keyboardHidden"
            >

        </activity>

        <activity
            android:screenOrientation="portrait"
            android:name=".CaseResidentVerificationActivity"
            android:windowSoftInputMode="stateHidden|adjustPan"
            android:theme="@style/NormalTheme"
            android:configChanges="orientation|keyboard|keyboardHidden">

        </activity>

        <activity
            android:name=".CameraActivity"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".MapsActivity">

        </activity>
        <activity android:name=".LocationPhotoActivity"
            android:screenOrientation="portrait"/>


       <!-- <provider
            android:name=".NewContentProvider"
            android:authorities="com.covert.verify360"
            android:authorities="com.covert.verify360"
            android:exported="true" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.covert.verify360.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths">
            </meta-data>
        </provider>-->

        <receiver android:name=".NetworkChangleReceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"
                    tools:ignore="BatteryLife" />
            </intent-filter>
        </receiver>

    </application>

</manifest>